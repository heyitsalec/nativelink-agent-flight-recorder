{
  "schema_version": "nlfr.demo.scenario.v1",
  "scenario_id": "nondeterministic-test-change",
  "title": "Nondeterministic test change",
  "parent_issue": "PER-998",
  "child_issue": "PER-1001",
  "provenance_note": "Synthetic simulated-agent metadata for the NLFR demo. This is not captured real AI provenance.",
  "simulated_agent": {
    "kind": "simulated_v1",
    "name": "demo-flaky-worker",
    "input_signal": "Add a test that appears to check scheduler jitter but is intentionally flaky."
  },
  "change_class": "failing_or_nondeterministic_test",
  "workload": {
    "repo_root": "demo/bazel-monorepo",
    "bazel_targets": [
      "//tasks:priority_test"
    ],
    "affected_paths": [
      "tasks/priority_test.py"
    ]
  },
  "patch": {
    "format": "unified_diff",
    "applies_to": "demo/bazel-monorepo",
    "diff": "diff --git a/tasks/priority_test.py b/tasks/priority_test.py\n--- a/tasks/priority_test.py\n+++ b/tasks/priority_test.py\n@@ -1,4 +1,5 @@\n from tasks.priority import describe_task, priority_band\n+import random\n \n \n def test_priority_band_marks_urgent_work():\n@@ -7,3 +8,7 @@\n \n def test_describe_task_keeps_leaf_label_readable():\n     assert describe_task(\"NLFR-3\", 72) == \"NLFR-3: normal\"\n+\n+\n+def test_scheduler_jitter_is_stable():\n+    assert random.choice([True, False])\n"
  },
  "expected_result": {
    "run_expectation": "may_pass_or_fail_without_code_changes",
    "risk": "high",
    "why": "The patch introduces randomness into the test result. A single run can only prove the observed outcome for that run."
  },
  "proof_claims": [
    {
      "claim_id": "demo.patch.flaky-test-introduced",
      "statement": "The candidate patch introduces an explicitly nondeterministic test body.",
      "source_kind": "simulated_v1",
      "confidence": "medium",
      "evidence_refs": [
        "scenario:nondeterministic-test-change",
        "patch:tasks/priority_test.py"
      ],
      "redaction_state": "safe"
    },
    {
      "claim_id": "demo.repeated-run-required",
      "statement": "A real recorder should require repeated observed outcomes before claiming nondeterminism.",
      "source_kind": "future",
      "confidence": "unknown",
      "evidence_refs": [
        "future:bazel-test-attempts://tasks:priority_test"
      ],
      "redaction_state": "unknown"
    }
  ]
}
