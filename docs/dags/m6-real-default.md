# M6 — Real default projection polish

Linear: PER-1066 (proposed) · Parent: PER-1058

## Objective

Close demo/proof gap; evaluator sees `collectable_v1` first, not fixtures.

## Status

Mostly delivered (dogfood DAG B). Remaining polish only — **never blocks other milestones**.

## Deliverables

- README/WALKTHROUGH callouts for `canvas-dev` default projection
- Optional fixture-fallback banner in canvas
- Docs label real vs simulated paths

## Non-blocking rule

M5/M7/M8/M9 proceed regardless of M6 completion state.
