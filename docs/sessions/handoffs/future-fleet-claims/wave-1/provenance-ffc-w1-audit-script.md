# Fleet claims audit script — Wave 1 provenance

**Worker:** `ffc-w1-audit-script`  
**Coordinator:** `coord-future-fleet-claims`  
**Date:** 2026-06-06  
**Host:** `/Users/alecbot/Documents/nativelink-agent-flight-recorder`  
**Status:** `DONE`

---

## Executive summary

Verified parent inline work on `scripts/fleet_claims_audit.py` and `scripts/fleet-claims-audit.sh`. The Python emitter already builds a research-only claim matrix from `UNSUPPORTED_REMOTE_EXECUTION_CLAIMS` with `source_kind: derived_v1`, `broker_rule`, and `supported_collectable_ceiling`. Fixed the only gap: shell wrapper lacked the executable bit (`chmod +x`).

---

## Inputs read

| Artifact | Path |
|----------|------|
| KOS routing | `docs/sessions/handoffs/unlock-wave/KOS-startup-routing.md` |
| Fleet claims DAG | `docs/dags/future-fleet-claims.md` |
| Claim source | `src/nlfr/projectors/remote_execution.py` |
| Python emitter | `scripts/fleet_claims_audit.py` |
| Shell wrapper | `scripts/fleet-claims-audit.sh` |

---

## Deliverables written

| File | Action |
|------|--------|
| `scripts/fleet_claims_audit.py` | Verified — no changes |
| `scripts/fleet-claims-audit.sh` | Verified — made executable (`chmod +x`) |
| `data/fleet-claims-audit/claim-matrix.json` | Generated by proof run |
| This file | Created |

---

## Matrix schema verified

- **Top-level:** `status: research_only`, `source_kind: derived_v1`, `confidence: high`, `redaction_state: safe`
- **broker_rule:** blocks fleet/scheduler UI workers without new direct evidence parsers
- **supported_collectable_ceiling:** remote executor config, worker endpoints, conditional worker identity
- **claims:** 5 rows mirroring `UNSUPPORTED_REMOTE_EXECUTION_CLAIMS` tuple order
  - `worker_identity` → `v1_policy: conditional` with parser + proof block metadata
  - `action_placement`, `queue_time`, `scheduler_assignment`, `load_distribution` → `out_of_scope` with explicit blockers

---

## Proof

```bash
./scripts/fleet-claims-audit.sh
python3 -c "import json; m=json.load(open('data/fleet-claims-audit/claim-matrix.json')); assert m['source_kind']=='derived_v1'"
# exit 0; 5 claims emitted
```

---

## Honesty / claim boundary

Research-only matrix — **no** fleet UI, **no** invented backend state. Canvas may only reflect Remote Boundary lens from this matrix; implement workers require new collectable parsers per `docs/dags/future-fleet-claims.md`.

---

## Return

```json
{
  "worker_id": "ffc-w1-audit-script",
  "status": "DONE",
  "handoff_dir": "docs/sessions/handoffs/future-fleet-claims/wave-1/",
  "artifacts": {
    "provenance": "provenance-ffc-w1-audit-script.md",
    "verified": ["scripts/fleet_claims_audit.py", "scripts/fleet-claims-audit.sh"],
    "generated": ["data/fleet-claims-audit/claim-matrix.json"]
  },
  "claims_touched": ["derived_v1_fleet_claim_matrix"],
  "blockers": []
}
```
