# T3 View Systems — nlfr.view-spec.v1, Catalog, Composer

**Worker:** `t3-r-view-systems` (explore)  
**Date:** 2026-06-06  
**Host:** `/Users/alecbot/Documents/nativelink-agent-flight-recorder`  
**Status:** `DONE`

## Executive summary

Today the canvas has **implicit views**: five `CanvasMode` values hardcoded in `App.tsx` with fixed projection fetches. Tier 3 design (coord-t3-design) must introduce **`nlfr.view-spec.v1`** — a versioned JSON document describing layout grids, component instances, and `projection_binding` contracts — without letting the UI invent backend state.

This handoff outlines the schema shape, a `component_kind` catalog mapped from the current monolith, the `projection_binding` contract, and a composer sketch for operator-authored views that remain projection-only.

---

## Current state vs target

| Concern | Today (`App.tsx`) | Target (T3-D) |
|---------|-------------------|---------------|
| View definition | `useState<CanvasMode>` | `ViewSpec` JSON document |
| Layout | CSS grid in `styles.css` + inline overlays | `grid` section in view spec |
| Data sources | 3 hardcoded fetch paths | `projection_binding` per component |
| Persistence | none | `localStorage` or file export (T3-I4) |
| Truth labels | per-node/block in rendering | required on every bound projection slice |

---

## `nlfr.view-spec.v1` — document outline

### Top-level envelope

```json
{
  "schema_version": "nlfr.view-spec.v1",
  "view_id": "tier1-demo-default",
  "title": "Tier 1 Agent Vision — default demo layout",
  "description": "Graph-primary layout with proof and compare rails",
  "generated_at": "2026-06-06T00:00:00.000000Z",
  "run_group": "canvas-dev",
  "layout": { },
  "components": [ ],
  "bindings": { },
  "source_kind": "derived_v1",
  "confidence": "medium",
  "evidence_refs": ["view-spec:tier1-demo-default"],
  "redaction_state": "safe"
}
```

### Required sections

#### 1. `layout` — grid shell

```json
{
  "layout": {
    "kind": "grid_shell_v1",
    "columns": "minmax(0,1fr) 440px",
    "rows": "auto auto minmax(0,1fr) auto",
    "regions": {
      "notice": { "grid_area": "notice", "slot": "banner" },
      "header": { "grid_area": "header", "slot": "topbar" },
      "primary": { "grid_area": "primary", "slot": "canvas" },
      "rail": { "grid_area": "rail", "slot": "inspector", "width_px": 440 },
      "operator": { "grid_area": "operator", "slot": "command_bar" }
    },
    "responsive": {
      "breakpoint_px": 720,
      "collapse_rail": "bottom_sheet"
    }
  }
}
```

Maps to Harmony's canvas-left / inspector-right pattern (see harmony provenance) while staying NLFR-sparse.

#### 2. `components` — ordered instances

Each entry:

```json
{
  "instance_id": "graph-main",
  "component_kind": "action_graph_canvas",
  "region": "primary",
  "props": {
    "show_truth_legend": true,
    "zoom_controls": true
  },
  "projection_binding": "binding.action_graph",
  "visible_when": { "mode": ["graph", "runway"] }
}
```

#### 3. `bindings` — projection paths and selectors

```json
{
  "bindings": {
    "binding.action_graph": {
      "projection_kind": "action_graph",
      "path": "/projections/action-graph.json",
      "required": true,
      "fallback": "fixture:sampleProjection"
    },
    "binding.proof_packet": {
      "projection_kind": "proof_packet",
      "path": "/projections/proof.json",
      "required": true,
      "fallback": "fixture:sampleProofPacket"
    },
    "binding.compare": {
      "projection_kind": "compare",
      "path": "/projections/compare-projection.json",
      "required": false
    },
    "binding.runway": {
      "projection_kind": "runway",
      "path": "/projections/runway.json",
      "required": false,
      "notes": "Orphan today — resolve in T3-I2"
    }
  }
}
```

#### 4. `modes` — lens registry (replaces `CanvasMode` enum)

```json
{
  "modes": [
    {
      "mode_id": "graph",
      "label": "Action Graph",
      "icon": "git-branch",
      "primary_component": "graph-main",
      "rail_component": "inspector-selected-node",
      "default_focus": "all"
    },
    {
      "mode_id": "compare",
      "label": "Compare Runs",
      "icon": "git-compare",
      "primary_component": "graph-main",
      "rail_component": "compare-lens",
      "requires_binding": "binding.compare"
    }
  ]
}
```

#### 5. Truth metadata on view spec itself

The view spec document is **operator/authored layout** — label `source_kind: derived_v1` when generated by composer from projections; `simulated_v1` for bundled demo specs. It must never claim collectable execution facts.

---

## `component_kind` catalog

Mapped from current `App.tsx` implementations → stable kinds for T3-D schema enum.

| `component_kind` | Current source | `projection_binding` | Output |
|------------------|----------------|----------------------|--------|
| `projection_notice` | `projectionNotice` memo | `binding.action_graph` + fallback flag | status banner |
| `topbar_summary` | header run strip | `binding.action_graph.summary` | run/node/cache counts |
| `mode_rail` | mode `IconButton` row | none (UI chrome) | mode switching |
| `action_graph_canvas` | SVG graph + D3 zoom | `binding.action_graph` | nodes/edges |
| `evidence_inspector` | `Inspector` | selected node from graph binding | node dossier |
| `validation_runway` | `RunwayOverlay` | graph nodes or `binding.runway` | timeline |
| `proof_constellation` | `ProofConstellation` | `binding.proof_packet` | in-canvas mini panel |
| `proof_drawer` | `ProofDrawer` | `binding.proof_packet` | block list |
| `proof_block_card` | `ProofBlockView` | slice of proof blocks | reusable card |
| `remote_boundary_lens` | `RemoteLens` | proof blocks by id filter | gated remote UI |
| `compare_lens` | `CompareLens` | `binding.compare` | dimension list |
| `compare_dimension_card` | `CompareDimensionView` | dimension slice | delta UI |
| `truth_legend` | `TruthLegend` | static | source kind key |
| `operator_command_bar` | operator section | none | local focus commands |
| `zoom_controls` | rail zoom buttons | none | D3 zoom API |

### Future kinds (T3 wave 4+ — not in v1 catalog)

| `component_kind` | Notes |
|------------------|-------|
| `run_group_selector` | needs retention index projection |
| `agent_provenance_card` | binds proof block `agent_provenance` |
| `audit_ledger_timeline` | Harmony port; needs event projection |
| `worker_lane_board` | requires role-labeled runs in projection |

---

## `projection_binding` contract

### Rules (normative for T3-D schema)

1. **Single source of truth** — Components may only render fields present in the bound projection JSON (or explicit static chrome).
2. **Binding resolution** — `{ path, projection_kind, selector? }` fetched once per view mount; optional `refresh_token` after local `nlfr graph export`.
3. **Selectors** — JSONPath-like pointers restricted to safe subset:
   - `$.nodes[?(@.id=='{selected}')]`
   - `$.blocks[?(@.kind=='agent_provenance')]`
   - `$.dimensions[?(@.id=='agent_provenance')]`
4. **Truth inheritance** — Rendered claims inherit `source_kind`, `confidence`, `evidence_refs`, `redaction_state` from the bound object; UI chrome uses `source_kind: derived_v1` only for layout metadata.
5. **No binding → no claim** — If `required: false` and fetch fails, component shows empty state (compare lens pattern); must not synthesize nodes.
6. **Fallback policy** — `fixture:*` fallbacks must set global `usingFixtureFallback` and show `projection_notice` (existing behavior).

### Binding descriptor schema (fragment)

```json
{
  "$id": "nlfr.projection_binding.v1",
  "type": "object",
  "required": ["projection_kind", "path"],
  "properties": {
    "projection_kind": {
      "enum": ["action_graph", "proof_packet", "compare", "runway", "run_group_index"]
    },
    "path": { "type": "string", "pattern": "^/" },
    "required": { "type": "boolean", "default": true },
    "selector": { "type": "string" },
    "fallback": { "type": "string", "pattern": "^(fixture:|none)" },
    "refresh": { "enum": ["manual", "on_run_group_change"], "default": "manual" }
  }
}
```

### Cross-binding joins (derived views only)

When a component needs two projections (e.g. remote lens):

```json
{
  "projection_binding": {
    "kind": "join_v1",
    "sources": ["binding.action_graph", "binding.proof_packet"],
    "join_fn": "remote_lens_model",
    "source_kind": "derived_v1",
    "confidence": "medium",
    "evidence_refs": ["binding.proof_packet:blocks.remote_execution"]
  }
}
```

`join_fn` must reference a **pure function registry** in `pageModel.ts` — not ad hoc component logic.

---

## View composer sketch

### Purpose

Let operators assemble `nlfr.view-spec.v1` documents from the component catalog without editing React. MVP: form-based composer exporting JSON file; not a live mutation path.

### Composer flow

```
┌──────────────┐    ┌─────────────────┐    ┌──────────────────┐
│ Load catalog │ → │ Pick template    │ → │ Bind projections │
│ (kinds+slots)│    │ (tier1-default)  │    │ (paths+required) │
└──────────────┘    └─────────────────┘    └──────────────────┘
         │                    │                        │
         v                    v                        v
┌──────────────┐    ┌─────────────────┐    ┌──────────────────┐
│ Add/remove   │ → │ Preview in canvas│ → │ Export view-spec │
│ components   │    │ (read-only)      │    │ .json + validate │
└──────────────┘    └─────────────────┘    └──────────────────┘
```

### MVP screens (T3-I4)

1. **Template picker** — `tier1-demo-default`, `graph-only`, `proof-review`
2. **Binding editor** — path inputs with validation against known projection kinds
3. **Mode editor** — reorder mode rail; map mode → primary/rail components
4. **Export** — writes `view-spec.json`; runs JSON schema validator
5. **Import** — canvas boot: `?view=tier1-demo-default` or `localStorage` key `nlfr.view-spec`

### Composer protocol (messages)

| Action | Request | Response |
|--------|---------|----------|
| `list_catalog` | — | `{ component_kinds: [...] }` |
| `list_templates` | — | `{ templates: [{ view_id, title }] }` |
| `validate_spec` | `{ spec }` | `{ ok, errors[] }` |
| `preview_spec` | `{ spec, projections_root }` | `{ render_plan }` |

Implementation: static TS module in `apps/canvas/src/composer/` — no server required for MVP.

### Validation gates

- JSON Schema `docs/design/view-spec.v1.schema.json` (T3-D deliverable)
- Every component instance must reference known `component_kind`
- Every `projection_binding` key must exist in `bindings`
- Warn when `run_group` in spec mismatches `action_graph.run_group`
- Reject specs that embed raw prompts, env vars, or secrets in `props`

---

## Default tier1 view spec (sketch)

Equivalent to current App.tsx behavior:

```json
{
  "schema_version": "nlfr.view-spec.v1",
  "view_id": "nlfr-default-v0",
  "title": "NLFR Default Canvas",
  "modes": ["graph", "runway", "proof", "remote", "compare"],
  "components": [
    { "instance_id": "notice", "component_kind": "projection_notice", "region": "notice" },
    { "instance_id": "topbar", "component_kind": "topbar_summary", "region": "header", "projection_binding": "binding.action_graph" },
    { "instance_id": "modes", "component_kind": "mode_rail", "region": "header" },
    { "instance_id": "graph", "component_kind": "action_graph_canvas", "region": "primary", "projection_binding": "binding.action_graph" },
    { "instance_id": "legend", "component_kind": "truth_legend", "region": "primary" },
    { "instance_id": "operator", "component_kind": "operator_command_bar", "region": "operator" }
  ],
  "bindings": {
    "binding.action_graph": { "projection_kind": "action_graph", "path": "/projections/action-graph.json", "required": true, "fallback": "fixture:sampleProjection" },
    "binding.proof_packet": { "projection_kind": "proof_packet", "path": "/projections/proof.json", "required": true, "fallback": "fixture:sampleProofPacket" },
    "binding.compare": { "projection_kind": "compare", "path": "/projections/compare-projection.json", "required": false }
  }
}
```

Mode-specific rails (`proof_drawer`, `compare_lens`, etc.) activate via `visible_when` without duplicating graph component.

---

## T3-D deliverables checklist (from charter)

| Deliverable | This handoff section |
|-------------|---------------------|
| `docs/design/view-spec.v1.schema.json` | Envelope + binding fragment above |
| Component catalog | `component_kind` table |
| Routing | `modes` registry + `visible_when` |
| Composer protocol | Composer sketch + message table |

---

## Anti-patterns to reject

1. **View spec as data source** — spec layout ≠ evidence; projections remain canonical
2. **Component-side fetch** — all fetches through binding resolver, not per-component `useEffect`
3. **Silent join inference** — no automatic agent/worker correlation across projections
4. **Persisting operator commands as claims** — command bar stays local draft
5. **Harmony four-tab clone** — modes are lenses on same run group, not separate products

---

## Source map

| Artifact | Path |
|----------|------|
| Current modes | `apps/canvas/src/types.ts` (`CanvasMode`) |
| Monolith | `apps/canvas/src/App.tsx` |
| Layout | `apps/canvas/src/layout.ts` |
| Coordinator charter | `docs/sessions/handoffs/tier1-agent-vision/wave-0/coordinator-charters.md` |
| Canvas audit | `docs/sessions/handoffs/tier1-agent-vision/wave-1/provenance-t3-canvas-audit.md` |
| Harmony patterns | `docs/sessions/handoffs/tier1-agent-vision/wave-1/provenance-t3-harmony-patterns.md` |
